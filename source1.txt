(define x "asd")
(print x)

(define ans 2520)
(define i 11)
(defun myboi (n) (
        (
            loop (
                (if (== i n) (nop) (break 0))
                (if (== i 20) (break 1) (nop))
                (set i (+ i 1))
            )
        )
    )
)
(   
    loop (
        (if (fucall myboi ans) (break 0) (set ans (+ ans 2520)))
        (set i 11)
    )
)